# Deploy Notes
- GitHub Pages source: gh-pages branch / (root)
- Live URL: https://iceferryling.github.io/weather-vision/
- Build: npm ci && npm run build
- Publish to gh-pages: bash ./deploy-gh-pages.sh
